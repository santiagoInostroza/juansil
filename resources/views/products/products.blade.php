<x-app-layout>
   
{{-- 
    <div class="container py-8">

        <h1 class="uppercase text-center text-3xl font-bold mb-10">{{$data->name}}</h1>

       <div class="grid grid-cols-1  md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 xl:gap-20">
            @foreach ($products as $producto)
                <livewire:producto :producto='$producto'/>
            @endforeach
        </div>

        <div class="grid grid-cols-1  md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4  gap-6">
           
        </div>

        <div class="mt-4">
            {{$products->links()}}
        </div>

    </div>
 --}}

</x-app-layout>